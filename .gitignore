node_modules/
.DS_Store
*.log
.env
dist/
build/
.vscode/
.idea/
.vercel

# Fontes/insumos locais (catálogo de produto, briefings) — não vão para o repositório nem para o deploy
input/

# Cache por sessao do hook do Impeccable — guarda caminhos absolutos da maquina
.impeccable/hook.cache.json
